SELECT
    customer_id,
    customer_name,
    SUM(revenue) AS total_revenue,
    COUNT(transaction_id) AS transaction_count
GROUP BY
    customer_id,
    customer_name
ORDER BY
    total_revenue DESC;
